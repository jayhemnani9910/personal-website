---
title: "CAG Deep Research"
summary: "A causal-adversarial deep-research engine: LangGraph plans a causal graph of a question, red and blue team agents investigate each causal edge in parallel, and a dialectical judge returns verdicts. Built on a hexagonal architecture with swappable search and LLM providers."
role: "Builder"
period: "2024"
domain: "AI/ML"
tags: ["langgraph", "agents", "research-automation", "causal"]
tech: ["Python", "LangGraph", "LangChain", "Pydantic", "Ollama", "Groq"]
github: "https://github.com/jayhemnani9910/revolu-idea"
links:
  demo: "https://jayhemnani9910.github.io/revolu-idea/"
challenge: "Single-LLM research tends to confirm whatever it first believes. The goal was a system that actively tries to falsify its own claims: model a question as a causal graph, then attack each causal link with a dedicated adversary before trusting it."
solution:
  - "A LangGraph workflow (CAG, Causal-Adversarial Graph) that plans a causal DAG, then investigates each edge with a parallel red team (adversary) and blue team (supporter)."
  - "A dialectical judge weighs the two sides on scientific criteria and returns a structured verdict: VERIFIED, FALSIFIED, or UNCLEAR, with a confidence score."
  - "Hexagonal architecture (ports + adapters) so search engines (DuckDuckGo, Tavily, Exa) and LLM providers (Ollama, Groq, GitHub Models) are swappable."
impact:
  - "Falsification-first design: every causal claim faces a dedicated adversary before it is accepted, instead of one model synthesising unchecked."
  - "Runs entirely on free backends (DuckDuckGo + Ollama or GitHub Models) or scales up to paid search and faster cloud LLMs, with no code changes."
  - "Safety rails throughout: recursion-depth caps, per-edge investigation limits, loop detection via action hashes, and an auditor that decides loop-or-finish."
featured: true
priority: 5
deepDive:
  context: "Most LLM research tools are confirmation engines: ask a question, get a confident synthesis, rarely an argument against it. That is the opposite of how good research works, where a claim is only trusted after someone has actually tried to break it. CAG (Causal-Adversarial Graph) is built around that idea, a Popperian falsification loop. It turns a question into a causal graph of hypothesised cause-and-effect links, then, for each link, runs an adversary whose only job is to find contradicting evidence alongside a supporter looking for confirming evidence. A judge weighs both and rules. The output is not a single narrative but a set of causal claims, each tagged with a verdict and the evidence on both sides."
  architecture: "The system is a LangGraph state machine over a shared ResearchState whose centre is a causal graph (the evolving 'world model'). A Causal Planner turns the query into a causal DAG of hypothesised edges. An Edge Selector picks the next unresolved edge. The Investigate step runs two agents concurrently (asyncio.gather): an Adversary (red team) searching for contradicting evidence and a Supporter (blue team) searching for supporting evidence, writing into separate evidence buffers that custom LangGraph reducers merge without clobbering each other. A Dialectical Judge then weighs the two sides on scientific criteria (credibility, recency, methodology, replication, effect size, confounders) and returns a structured verdict (VERIFIED / FALSIFIED / UNCLEAR) with confidence, updating the graph. An Auditor enforces safety, recursion-depth caps, per-edge investigation limits, loop detection via action hashes, node-visit counting, and routes back to the Selector or on to the Writer, which synthesises the final verdict-tagged report. Routing is done with explicit Command(goto=...) returns rather than conditional edges, a deliberate workaround for hangs observed with LangGraph 1.0.x conditional edges. Everything external lives behind hexagonal ports: LLM, search, and storage adapters are injected, so the same engine runs on free or paid backends."
  dataFlow:
    - step: "Plan"
      detail: "The Causal Planner turns the query into a causal DAG of hypothesised cause-to-effect edges."
    - step: "Select"
      detail: "The Edge Selector picks the next unresolved edge, capped by a per-edge investigation limit."
    - step: "Investigate (parallel)"
      detail: "Adversary (red) and Supporter (blue) run concurrently: one hunts contradicting evidence, the other supporting evidence."
    - step: "Judge"
      detail: "The Dialectical Judge weighs both sides on scientific criteria and returns VERIFIED / FALSIFIED / UNCLEAR with a confidence score, updating the graph."
    - step: "Audit"
      detail: "The Auditor checks gaps, recursion depth, visit counts, and loop hashes, then routes back to Select or on to Write."
    - step: "Write"
      detail: "The Writer synthesises a report where each causal claim carries its verdict and the evidence on both sides."
  components:
    - name: "Causal Planner"
      purpose: "Turns the query into a causal DAG of hypothesised cause-and-effect edges, the system's world model."
    - name: "Edge Selector"
      purpose: "Picks the next unresolved edge to investigate and enforces a per-edge investigation cap."
    - name: "Adversary (red team)"
      purpose: "Searches specifically for evidence that contradicts or falsifies the focus edge."
    - name: "Supporter (blue team)"
      purpose: "Searches for evidence that supports the focus edge; runs in parallel with the adversary."
    - name: "Dialectical Judge"
      purpose: "Weighs both sides on scientific criteria and returns a structured verdict (VERIFIED / FALSIFIED / UNCLEAR) with confidence, then updates the graph."
    - name: "Auditor"
      purpose: "Safety and control: recursion-depth caps, loop detection via action hashes, node-visit counting, and loop-or-finish routing."
    - name: "Writer"
      purpose: "Synthesises the final verdict-tagged report from the resolved causal graph."
    - name: "Ports + adapters (hexagonal)"
      purpose: "LLM, search, and storage behind interfaces, with adapters for DuckDuckGo / Tavily / Exa and Ollama / OpenAI-compatible / Groq / GitHub Models."
  keyDecisions:
    - decision: "Causal-adversarial (Popperian) design over a single-LLM synthesiser"
      reasoning: "A question is modelled as a causal graph, and each edge is attacked by a dedicated adversary before being accepted. Trying to falsify a claim catches confident-but-wrong synthesis that a single model would wave through."
      tradeoff: "Far more LLM and search calls per question than a one-shot answer, which is why recursion depth and per-edge investigations are capped."
    - decision: "Run adversary and supporter in parallel, then judge"
      reasoning: "Red and blue teams run concurrently (asyncio.gather) so each edge gets both perspectives quickly, and a separate judge synthesises a verdict, no single agent both argues and rules."
      tradeoff: "Parallel writes need care: supporting and contradicting evidence go to separate buffers with custom LangGraph reducers so concurrent updates do not clobber each other."
    - decision: "Command(goto=...) routing instead of conditional edges"
      reasoning: "LangGraph 1.0.x conditional edges hung in this environment, so nodes return explicit Command(goto=...) to route (e.g. auditor decides selector vs writer). It is deterministic and debuggable."
      tradeoff: "Routing logic lives in node wrappers rather than as declarative graph edges, slightly less of the graph is visible at a glance."
    - decision: "Hexagonal ports and adapters for search and LLMs"
      reasoning: "Search engines and model providers sit behind ports, so the engine runs on entirely free backends (DuckDuckGo + Ollama or GitHub Models) for development and swaps to paid search or faster cloud LLMs for quality, by configuration."
      tradeoff: "More upfront interface and adapter code than calling an SDK directly."
  codeSnippets:
    - title: "The CAG graph: a falsification loop"
      language: "python"
      code: |
        class CAGGraphBuilder:
            """Causal-Adversarial Graph (CAG): a Popperian falsification engine.
            1. Planner   -> builds a causal DAG from the query
            2. Selector  -> picks the next edge to investigate
            3. Adversary + Supporter -> parallel red / blue research
            4. Judge     -> resolves the conflict, updates the graph
            5. loop to Selector until edges resolve or max depth
            6. Writer    -> synthesises the final report
            """
            def build(self) -> StateGraph:
                wf = StateGraph(ResearchState)
                wf.add_node("planner", self._run_planner)
                wf.add_node("selector", self._run_selector)
                wf.add_node("investigate", self._run_parallel_investigation)
                wf.add_node("judge", self._run_judge)
                wf.add_node("auditor", self._run_auditor)
                wf.add_node("writer", self._run_writer)
                wf.add_edge(START, "planner")
                wf.add_edge("planner", "auditor")
                wf.add_edge("investigate", "judge")
                wf.add_edge("judge", "auditor")
                wf.add_edge("writer", END)
                # routing via Command(goto=...) inside nodes: a deliberate
                # workaround for LangGraph 1.0.x conditional-edge hangs
                return wf.compile()
      explanation: "The whole engine in one place. Note the honest detail: conditional edges hung on this LangGraph version, so the auditor and selector route by returning Command(goto=...) instead, which kept the loop deterministic and debuggable."
    - title: "Red team + blue team, in parallel"
      language: "python"
      code: |
        async def _run_parallel_investigation(self, state):
            """Adversary (red) and Supporter (blue) research the same edge at once."""
            adversary, supporter = await asyncio.gather(
                self._run_adversary(state),   # hunts contradicting evidence
                self._run_supporter(state),   # hunts supporting evidence
            )
            return {
                "contradicting_evidence": adversary.get("contradicting_evidence", []),
                "supporting_evidence":    supporter.get("supporting_evidence", []),
                # node_visit_counts / action_hashes merged via state reducers
            }
      explanation: "Both teams investigate the same causal edge concurrently and write to separate evidence buffers. Custom reducers on the ResearchState merge the two parallel updates so neither side's writes are lost, which is the tricky part of doing this in LangGraph."
    - title: "The dialectical judge's structured verdict"
      language: "python"
      code: |
        class JudgmentOutput(BaseModel):
            verdict: Literal["VERIFIED", "FALSIFIED", "UNCLEAR"]
            confidence: float = Field(ge=0.0, le=1.0)
            reasoning: str
            key_supporting_points: list[str] = []
            key_contradicting_points: list[str] = []
            methodological_concerns: list[str] = []

        # The judge weighs evidence on explicit criteria:
        # credibility, recency, methodology, replication,
        # effect size, confounders -> one structured verdict per edge.
      explanation: "The judge does not return a vibe. It emits a typed verdict with confidence and the key points on each side, decided against named scientific criteria, so every accepted causal link is auditable."
  metrics:
    - value: "7 nodes"
      label: "in the graph"
      context: "planner · selector · adversary · supporter · judge · auditor · writer"
    - value: "Red / Blue"
      label: "per causal edge"
      context: "adversary vs supporter, then a dialectical judge"
    - value: "Hexagonal"
      label: "ports + adapters"
      context: "swappable search and LLM providers"
    - value: "5+"
      label: "providers"
      context: "DuckDuckGo / Tavily / Exa · Ollama / Groq / GitHub Models"
  learnings:
    - "Falsification beats confirmation. Giving each causal edge a dedicated adversary surfaced contradictions that a single synthesising model would have smoothed over. The adversary is the most valuable agent in the system."
    - "Parallelism is easy; merging parallel state is not. Running red and blue teams with asyncio.gather was trivial, but getting their concurrent writes into LangGraph state without clobbering took custom reducers and separate evidence buffers."
    - "Sometimes the honest fix is a workaround. Conditional edges hung on this LangGraph version, so routing moved into Command(goto=...) returns. Less elegant, but deterministic and debuggable beat clever-but-hanging."
    - "Hexagonal architecture earned its keep. Because search and LLMs sit behind ports, the same engine runs free on DuckDuckGo + Ollama or scales to paid search and cloud models by config, which made iterating cheap."
    - "Agent loops need hard rails. Recursion-depth caps, per-edge limits, node-visit counts, and action-hash loop detection are what stop a self-directed research loop from running forever."
  futureWork:
    - "Persist and visualise the causal graph so a reader can see which edges were verified, falsified, or left unclear."
    - "Wire the live demo to real runs with grounded citations; the current published sample is a static shape preview."
    - "Add a calibration pass that checks whether judge confidence scores track real-world correctness."
---
# Overview
CAG (Causal-Adversarial Graph) is a deep-research engine built as a Popperian falsification loop. A LangGraph workflow turns a question into a causal graph, then investigates each causal edge with a red team (adversary) and blue team (supporter) running in parallel; a dialectical judge weighs both and returns a verdict (VERIFIED / FALSIFIED / UNCLEAR) with confidence, while an auditor enforces depth caps and loop detection and the writer synthesises a verdict-tagged report. It is built on a hexagonal architecture, so search engines (DuckDuckGo, Tavily, Exa) and LLM providers (Ollama, Groq, GitHub Models) are swappable by configuration.
